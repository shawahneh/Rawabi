<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techcamp.aauj.rawabi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".activities.DashbordActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CarpoolActivity"
            android:theme="@style/AppTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".activities.LoginRegisterActivity"
            android:theme="@style/AppTheme.No" />
        <activity android:name=".activities.PickerActivity" />
        <activity
            android:name=".activities.UserTypeActivity"
            android:theme="@style/AppTheme.No" />
        <activity android:name=".activities.PendingActivity" />
        <activity
            android:name=".activities.MeetingMapActivity"
            android:label="Choosing Meeting Location" />
        <activity
            android:name=".activities.carpoolActivities.MapActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.carpoolActivities.MapRiderActivity" />
        <activity android:name=".activities.carpoolActivities.ProfileActivity" android:label="@string/my_profile"/>
        <activity android:name=".activities.carpoolActivities.ListActivity" />
        <activity android:name=".activities.carpoolActivities.RideDetailActivity" android:label="@string/my_rides" />
        <activity android:name=".activities.carpoolActivities.MyRiderActivity" />
        <activity android:name=".activities.carpoolActivities.MyJourneysActivity" />
        <activity android:name=".activities.carpoolActivities.JourneyDetailActivity" android:label="@string/my_journeys"></activity>
        <activity android:name=".activities.carpoolActivities.MapDriverActivity" />
    </application>

</manifest>